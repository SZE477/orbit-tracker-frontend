import React from 'react';

// This file is a placeholder for context providers (e.g., ThemeProvider, AuthProvider)
// For this project, we don't have complex providers yet, but it's good practice to have this shell.

export const AppProviders = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};