import React from 'react';
/**
 * React Query Devtools component that only loads in development
 * This helps avoid bundle size issues and import errors in production
 */
export declare const QueryDevtools: React.FC;
